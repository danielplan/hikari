{
  "name": "hikari",
  "version": "0.0.0",
  "homepage": "http://danielplan.github.io/hikari",
  "scripts": {
    "dev": "vite",
    "build": "npm run compile && tsc && vite build",
    "serve": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist",
    "compile": "cd src/editor/renderEngine && make"
  },
  "devDependencies": {
    "gh-pages": "^3.2.3",
    "sass": "^1.43.4",
    "typescript": "^4.3.2",
    "vite": "^2.6.4"
  }
}
